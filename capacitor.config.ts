import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salaheddine.schedule',
  appName: 'التوقيت المدرسي',
  webDir: 'www'
};

export default config;
